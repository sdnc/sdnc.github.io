import Typed from 'typed.js';

var options = {
  strings: ["I build websites", "I make cool programs", "I'm a full-stack developer and business analyst"],
  typeSpeed: 120,
  backSpeed: 50,
  backDelay: 1500
  // loop: true
};

var typed = new Typed('#headline', options);
